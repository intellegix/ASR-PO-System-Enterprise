{
  "name": "p.o-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd web && npm ci && npx prisma generate && npm run build",
    "start": "cd web && npm start",
    "start:minimal": "node minimal-server.js",
    "start:fallback": "node minimal-server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "artillery": "^2.0.27"
  }
}
